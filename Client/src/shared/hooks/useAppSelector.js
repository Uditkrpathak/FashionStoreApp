// src/shared/hooks/useAppSelector.js
import { useSelector } from 'react-redux';
/** Typed selector hook — use this instead of raw useSelector everywhere. */
export const useAppSelector = useSelector;
