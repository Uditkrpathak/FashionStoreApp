// src/shared/hooks/useAppDispatch.js
import { useDispatch } from 'react-redux';
/** Typed dispatch hook — use this instead of raw useDispatch everywhere. */
export const useAppDispatch = () => useDispatch();
